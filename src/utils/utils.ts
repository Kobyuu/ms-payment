// Calcula el precio total multiplicando precio por cantidad
export const calculateTotalPrice = (price: number, quantity: number): number => {
  // Retorna el resultado de multiplicar precio unitario por cantidad
  return price * quantity;
};